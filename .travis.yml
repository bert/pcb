language: c

os:
  - linux

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - graphviz
      - autopoint
      - intltool
      - texinfo
      - libgd2-xpm
      - libgd2-xpm-dev
      - lesstif-bin
      - libghc-gtkglext-prof
      - libsqlite3-dev
      - libdbus-glib-1-dev
      - libdbus-1-dev

before_install:
  - sudo apt-get -qq update

script: scripts/build.sh

# blacklist
branches:
  except:
    - master

# whitelist
branches:
  only:
    - devel

notifications:
  recipients:
    - bert.timmerman@xs4all.nl
  email:
    on_success: change
    on_failure: always

