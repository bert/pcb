language: c

os:
  - linux

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - graphviz
      - autopoint 0.19.3
      - autoconf 2.69
      - intltool
      - texinfo
      - texlive-full
      - libgd2-noxpm
      - libgd2-noxpm-dev
      - lesstif2
      - lesstif2-dev
      - libghc-gtkglext-prof
      - libsqlite3-dev
      - libdbus-glib-1-dev
      - libdbus-1-dev
      - desktop-file-utils
      - gerbv
      - geda-gschem

before_install:
  - sudo apt-get -qq update

script: scripts/build.sh

# blacklist
branches:
  except:
    - master

# whitelist
branches:
  only:
    - devel

notifications:
  recipients:
    - bert.timmerman@xs4all.nl
  email:
    on_success: change
    on_failure: always

